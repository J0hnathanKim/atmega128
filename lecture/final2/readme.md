<11/27>  
8장 - ctc모드(64 슬라이드부터)  
ctc - 오버플로우 발생 x  
8-8,8-9(모터제어) 스킵  
81슬라이드 공부하고 나머진 스킵  
[9장 16비트 타이머/카운터 - 8비트 카운터를 두개 이용]  
10슬라이드 잘 기억  
<12/4>  
TOP값 결정 레지스터 - OCRn ICRn (ctc모드가 아님에도 불구하고 top값 할당가능)  
비교일치 인터럽트  
top값과 비교일치 인터럽트가 발생하는 값을 달리할 수 있음.  
TCCRnx의 역할  
timsk, etimsk 레지스터 - 어디를 어떻게 세팅해줘야하는가?(extended는 3번 타이머에 관련한거 - 1, 2번은 타이머마스크 레지스터에 있다)  
tifr, etifr 레지스터 - 해당 플래그가 1로 셋되어 인터럽트 서비스 루틴 진입 대기  
인터럽트 사용하지 않으려면 레지스터를 어떻게 세팅해야할까?  
비트 출력 파형 발생모드(26슬라이드) - 고속pwm, 위상정정 pwm, 위상 주파수 정정, ctc모드에서 top값 설정가능 -> OCRn값은 즉시 변경되지 않는다. 특정 버퍼에 들어갔다가 특정 조건이 되면 버퍼의 값이  OCRn값으로 할당됨.  
위상주파수 정정모드(8비트에선 존재x) - 위상정정 모드랑 OCRn 갱신 시기가 다름, 한 주기가 bottom to bottom  
고속 PWM 모드에서 OCn신호 출력  
COMnx1~COMnx0 설정과 OCn출력극성  
비교일치 인터럽트  
OCIEnx를 활성화하지 않은 경우에도 OCnx핀 펄스 출력 작동  
최댓값에서 PWM 연속 신호 보장 COMnx1~COMnx0=(1,0) 일 경우  
28슬라이드 그림이해(1:0 = 3은 둘 다 1 1 로 세팅했다는 의미)  
35슬라이드 그림 이해 
33슬라이드 OCRnx = TCNTn 조건 만족시 어떤 일이 발생하는가? 비교일치가 발생한다. 비교일치가 발생하면 어떤 일이 발생하는지는 COM 비트 결정  
33슬라이드 맨 마지막 단락은 오타 top값이 0이라는 부분이 생략되어있음.  
16비트 - 1가지의 타이머에 3가지의 OCRn이 정의되어있음.  
top값 찍고 갱신 -> 38슬라이드 OCRn 갱신시점  
40 위상정정 vs 위상 주파수 정정 -> 갱신을 top이 아니라 bottom에서 한다.  
ctc모드 신호 출력, comn1~comn0 설정과 OCn출력 극성  
일반모드는 왜 65536 x 프리스케일인가? top값이 정해져있기때문  
실험 skip(45~49)  
50슬라이드 이후는 나가지 않았음.  







