<2주차>  
IR과 PC의 협업원리  
명령어 패치란 무엇인가?  
범용레지스터 -> 2개의 레지스터를 시리얼로 붙이는 이유  
두 개의 레지스터를 묶으면 활용을 더 크게 할 수 있다?  
범용레지스터 - 주소 레지스터 => 직접, 간접주소  
SRAM -> 연습장같은 존재  
데이터메모리 -> 일반모드에서 작업할 예정  
RISC vs CISC => ATmega128은 RISC방식  
프로그램 흐름 제어 명령어 => JMP k 까지는 문제 x / 함수를 만나면? 함수가 있는 곳으로 점프 -> return 0을 만난 후 다시 원위치  
신호레벨에서 움직이는가?  
직접, 간접연결 조건 => 간접연결(증폭기 사용)  
간접연결 유용함.  
비동기 통신? 직렬 통신(데이터 손실이 적다)? USART => 동기 비동기 가능

<5주차>  
발광다이오드 <- 일정 수준의 전류가 흘러들어가야 빛이 들어옴. 전류량 조절은 저항이 한다.  
VR = VAVR(4.5) - VD(3.25) = 1.25  
논리값과 I/O 핀의 디지털 전압레벨과 변환  
pin을 통해 들어오는 정보 => 0 아니면 1밖에 없다.  

<6주차>  
입/출력에 따라 풀업저항 세팅 다르게 해야함. -> DDxn, PORTxn  
정격전류  
트랜지스터를 이용한 스위칭: 다이렉트 vs 트렌지스터  
간접구동방식 vs 직접구동방식  
스위치: 전자스위치, 기계스위치  
ib, ic ?  
포화영역 vs 차단영역  
디지털I/O핀을 이용한 릴레이 구동  

<7주차>  
36슬라이드 - pnp(위에 두개), npn(아래 두개)
폴링(while같은 반복문으로 계속 상태확인) vs 인터럽트(이벤트 시 알람)  
상승에지 vs 하강에지 감지 코드  
레벨 vs 상승 vs 하강  
atmega -> 8bit짜리 (8bit짜리 레지스터 2개를 묶어서 사용함, 12bit를 표현하기 위해)  
EECR(레지스터)  
cli() -> 인터럽트 x  
시험범위 6-32슬라이드    

<11/13 - 8장 8비트 타이머/카운터>  
타이머 - 아마도 주기적인 신호를 디텍션?(타이머 vs 카운터 - 주기적, 비주기적?)  
카운터 - 특정 이벤트가 발생하는 횟수를 셈 -> 주기적인 클록을 세는 거라면 타이머라고 말함  
v = IR, p = v^2 / r  
cpu 부담이 없게 장치를 제어하기위해 타이머를 사용  
매우 정확한 시간을 측정할때 필요한게 타이머/카운터  
cpu -> 플립플롭이 아니라 순차논리, 그래서 클록이 필요함(조합 논리가 아님)  
TCCRn 레지스터2주차>  
IR과 PC의 협업원리  
명령어 패치란 무엇인가?  
범용레지스터 -> 2개의 레지스터를 시리얼로 붙이는 이유  
두 개의 레지스터를 묶으면 활용을 더 크게 할 수 있다?  
범용레지스터 - 주소 레지스터 => 직접, 간접주소  
SRAM -> 연습장같은 존재  
데이터메모리 -> 일반모드에서 작업할 예정  
RISC vs CISC => ATmega128은 RISC방식  
프로그램 흐름 제어 명령어 => JMP k 까지는 문제 x / 함수를 만나면? 함수가 있는 곳으로 점프 -> return 0을 만난 후 다시 원위치  
신호레벨에서 움직이는가?  
직접, 간접연결 조건 => 간접연결(증폭기 사용)  
간접연결 유용함.  
비동기 통신? 직렬 통신(데이터 손실이 적다)? USART => 동기 비동기 가능

<5주차>  
발광다이오드 <- 일정 수준의 전류가 흘러들어가야 빛이 들어옴. 전류량 조절은 저항이 한다.  
VR = VAVR(4.5) - VD(3.25) = 1.25  
논리값과 I/O 핀의 디지털 전압레벨과 변환  
pin을 통해 들어오는 정보 => 0 아니면 1밖에 없다.  

<6주차>  
입/출력에 따라 풀업저항 세팅 다르게 해야함. -> DDxn, PORTxn  
정격전류  
트랜지스터를 이용한 스위칭: 다이렉트 vs 트렌지스터  
간접구동방식 vs 직접구동방식  
스위치: 전자스위치, 기계스위치  
ib, ic ?  
포화영역 vs 차단영역  
디지털I/O핀을 이용한 릴레이 구동  

<7주차>  
36슬라이드 - pnp(위에 두개), npn(아래 두개)
폴링(while같은 반복문으로 계속 상태확인) vs 인터럽트(이벤트 시 알람)  
상승에지 vs 하강에지 감지 코드  
레벨 vs 상승 vs 하강  
atmega -> 8bit짜리 (8bit짜리 레지스터 2개를 묶어서 사용함, 12bit를 표현하기 위해)  
EECR(레지스터)  
cli() -> 인터럽트 x  
시험범위 6-32슬라이드    

<11/13 - 8장 8비트 타이머/카운터>  
타이머 - 아마도 주기적인 신호를 디텍션?(타이머 vs 카운터 - 주기적, 비주기적?)  
카운터 - 특정 이벤트가 발생하는 횟수를 셈 -> 주기적인 클록을 세는 거라면 타이머라고 말함  
v = IR, p = v^2 / r  
cpu 부담이 없게 장치를 제어하기위해 타이머를 사용  
매우 정확한 시간을 측정할때 필요한게 타이머/카운터  
cpu -> 플립플롭이 아니라 순차논리, 그래서 클록이 필요함(조합 논리가 아님)  
TCCRn 레지스터 -> 일반모드, ctc모드, pwm 모드 ...  
정밀한 시간 측정 - 프리스케일러 x  
엣지디텍터  
tcnt -> 연습장 같은거  
ocrn -> 멕시멈 벨류 지정  
모드에 따른 오버플로우  
일반: 255까지가 표현 가능한 최대수(8비트라서)  
ctc모드: ocr에 들어있는 내용까지만 증가(ocr은 8비트 - 255 maximum) - 오버플로우 발생 x  
3: 8비트 타이머/카운터 n 오버플로우 인터럽트 전까지 진도나감  

<11/20>  
ctc 모드 오버플로우 x  
3: 8비트 타이머/카운터 n 오버플로우 인터럽트 전까지 진도나감   <<- 시험에 나온다 (마컨8 20슬라이드)  
오버플로우 인터럽트 발생주기  
프리스케일 중요(프리스케일에 따라 pcnp 어떻게 변하는지 알아둘것)  
led 점멸주기 계산(30 슬라이드)  

비교일치(각 레지스터의 역할, 어떤 일이 발생하는지)  
고속 pwm 모드 -> 주기를 변경하고 싶다면?  
오버플로 발생 -> 고속 pwm의 오버플로우 발생조건  
과제: OCRn을 0으로 설정해도 0% 듀티비가 되지 않음. , 255로 설정해도 100% 듀티비가 되지않음 -> 이유는 왜?? (11/30)까지  
위상 정정 pwm 모드화영역 vs 차단영역  
디지털I/O핀을 이용한 릴레이 구동  

<7주차>  
36슬라이드 - pnp(위에 두개), npn(아래 두개)
폴링(while같은 반복문으로 계속 상태확인) vs 인터럽트(이벤트 시 알람)  
상승에지 vs 하강에지 감지 코드  
레벨 vs 상승 vs 하강  
atmega -> 8bit짜리 (8bit짜리 레지스터 2개를 묶어서 사용함, 12bit를 표현하기 위해)  
EECR(레지스터)  
cli() -> 인터럽트 x  
시험범위 6-32슬라이드    

<11/13 - 8장 8비트 타이머/카운터>  
타이머 - 아마도 주기적인 신호를 디텍션?(타이머 vs 카운터 - 주기적, 비주기적?)  
카운터 - 특정 이벤트가 발생하는 횟수를 셈 -> 주기적인 클록을 세는 거라면 타이머라고 말함  
v = IR, p = v^2 / r  
cpu 부담이 없게 장치를 제어하기위해 타이머를 사용  
매우 정확한 시간을 측정할때 필요한게 타이머/카운터  
cpu -> 플립플롭이 아니라 순차논리, 그래서 클록이 필요함(조합 논리가 아님)  
TCCRn 레지스터2주차>  
IR과 PC의 협업원리  
명령어 패치란 무엇인가?  
범용레지스터 -> 2개의 레지스터를 시리얼로 붙이는 이유  
두 개의 레지스터를 묶으면 활용을 더 크게 할 수 있다?  
범용레지스터 - 주소 레지스터 => 직접, 간접주소  
SRAM -> 연습장같은 존재  
데이터메모리 -> 일반모드에서 작업할 예정  
RISC vs CISC => ATmega128은 RISC방식  
프로그램 흐름 제어 명령어 => JMP k 까지는 문제 x / 함수를 만나면? 함수가 있는 곳으로 점프 -> return 0을 만난 후 다시 원위치  
신호레벨에서 움직이는가?  
직접, 간접연결 조건 => 간접연결(증폭기 사용)  
간접연결 유용함.  
비동기 통신? 직렬 통신(데이터 손실이 적다)? USART => 동기 비동기 가능

<5주차>  
발광다이오드 <- 일정 수준의 전류가 흘러들어가야 빛이 들어옴. 전류량 조절은 저항이 한다.  
VR = VAVR(4.5) - VD(3.25) = 1.25  
논리값과 I/O 핀의 디지털 전압레벨과 변환  
pin을 통해 들어오는 정보 => 0 아니면 1밖에 없다.  

<6주차>  
입/출력에 따라 풀업저항 세팅 다르게 해야함. -> DDxn, PORTxn  
정격전류  
트랜지스터를 이용한 스위칭: 다이렉트 vs 트렌지스터  
간접구동방식 vs 직접구동방식  
스위치: 전자스위치, 기계스위치  
ib, ic ?  
포화영역 vs 차단영역  
디지털I/O핀을 이용한 릴레이 구동  

<7주차>  
36슬라이드 - pnp(위에 두개), npn(아래 두개)
폴링(while같은 반복문으로 계속 상태확인) vs 인터럽트(이벤트 시 알람)  
상승에지 vs 하강에지 감지 코드  
레벨 vs 상승 vs 하강  
atmega -> 8bit짜리 (8bit짜리 레지스터 2개를 묶어서 사용함, 12bit를 표현하기 위해)  
EECR(레지스터)  
cli() -> 인터럽트 x  
시험범위 6-32슬라이드    

<11/13 - 8장 8비트 타이머/카운터>  
타이머 - 아마도 주기적인 신호를 디텍션?(타이머 vs 카운터 - 주기적, 비주기적?)  
카운터 - 특정 이벤트가 발생하는 횟수를 셈 -> 주기적인 클록을 세는 거라면 타이머라고 말함  
v = IR, p = v^2 / r  
cpu 부담이 없게 장치를 제어하기위해 타이머를 사용  
매우 정확한 시간을 측정할때 필요한게 타이머/카운터  
cpu -> 플립플롭이 아니라 순차논리, 그래서 클록이 필요함(조합 논리가 아님)  
TCCRn 레지스터 -> 일반모드, ctc모드, pwm 모드 ...  
정밀한 시간 측정 - 프리스케일러 x  
엣지디텍터  
tcnt -> 연습장 같은거  
ocrn -> 멕시멈 벨류 지정  
모드에 따른 오버플로우  
일반: 255까지가 표현 가능한 최대수(8비트라서)  
ctc모드: ocr에 들어있는 내용까지만 증가(ocr은 8비트 - 255 maximum) - 오버플로우 발생 x  
3: 8비트 타이머/카운터 n 오버플로우 인터럽트 전까지 진도나감  

<11/20>  
ctc 모드 오버플로우 x  
3: 8비트 타이머/카운터 n 오버플로우 인터럽트 전까지 진도나감   <<- 시험에 나온다 (마컨8 20슬라이드)  
오버플로우 인터럽트 발생주기  
프리스케일 중요(프리스케일에 따라 pcnp 어떻게 변하는지 알아둘것)  
led 점멸주기 계산(30 슬라이드)  

비교일치(각 레지스터의 역할, 어떤 일이 발생하는지)  
고속 pwm 모드 -> 주기를 변경하고 싶다면?  
오버플로 발생 -> 고속 pwm의 오버플로우 발생조건  
과제: OCRn을 0으로 설정해도 0% 듀티비가 되지 않음. , 255로 설정해도 100% 듀티비가 되지않음 -> 이유는 왜?? (11/30)까지  
위상 정정 pwm 모드  
-62슬라이드까지 진도나감  
과제: (마컨8)코드 2-1, 2-2를 해석,   
      예제3 cpu클럭이 16만hz일때 똑같이 작동할 수 있도록 분주비나 cpu클럭을 계산해야함. 레지스터를 어떻게 세팅할지도 알아야함. 계산과정과 레지스터를 어떻게 세팅할지. (원본: 1MHz기준으로 돌아가는중) -> 레지스터, 분주비 계산과정  






